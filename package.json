{
  "name": "sonolus-bandori-skin-generator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p .",
    "start": "node dist/index.js",
    "generate": "npm run build && npm start",
    "generate:sample": "npm run build && node dist/index.js --sample",
    "list-plans": "npm run build && node dist/index.js --list-plans"
  },
  "dependencies": {
    "@sonolus/core": "^7.15.1",
    "fs-extra": "^11.3.2",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
